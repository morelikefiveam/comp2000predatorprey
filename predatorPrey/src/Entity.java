public class Entity {
     int x;
     int y; 
     boolean isFood;

     public Entity(int x, int y, boolean isFood) {
         this.x = x;
         this.y = y;
         this.isFood = isFood;
     }
}